import { ExpansionType } from './constants';
import { ExpansionTypeProps } from './types';

class TableResizer {
    public static getCellsSizes(params: ExpansionTypeProps) {
        // Auto = 'Auto',
        // Responsive = 'Responsive',
        // Fixed = 'Fixed',
        // Custom = 'Custom',
    }
}

export { TableResizer };
